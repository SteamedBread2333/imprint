# 2026-09-23 imprint 核心功能优化 + embed 语义查重插件

## 完成项（imprint 主仓库，未提交）

### P0-1 schema v2 迁移（原 P0 硬伤）
- `internal/vault/sqlite/store.go`：`schemaVersion` "1"→"2"，新增 `migrate()`。用 `PRAGMA table_info` 探测，兼容 v1.2.1 库（补 `query_local` 列）与 v1.3.2 库（补 `rule_stats.base_confidence`）。旧 `last_touched_at` 数据迁入 `rule_stats.last_confirmed_at`；版本高于二进制时报清晰错误（不再叫人删库）。
- 测试：`migrate_test.go` —— 旧库升级不丢数据 + 新版库拒绝打开。

### P0-2 零命中不出局
- `find.go` `rankScore`：`qs==0` 不再 `return 0,false`，改 `zeroHitFloor(0.3)*confidence` 排末尾。scope AND 硬过滤保留。

### P1 衰减解耦 cron
- `SweepActive` 重写为幂等连续折算：`confidence = max(0, base - λ·idle_days)`，λ=decayAmount/decayDays，idle 基于 `max(last_confirmed_at, last_recalled_at)`（检索也算活跃 = testing effect）。
- 新增 `rule_stats.base_confidence`：add/reinforce/supersede/PutRecord 维护，sweep 只改 confidence 不改 base → 每天跑/每月跑/只跑一次结果一致。
- **踩坑**：idle≈0 时浮点噪声触发微衰减（0.6875→0.6874999），acceptance 场景 12 挂了。加 `minIdleDays = 1/24` 修复。

### P1 conflicts_with 激活
- `penalizeConflicts()`：命中集内冲突对的分低者 ×0.5 后重排；查边失败静默跳过（emit `conflict_penalty_failed`）。在 `Find`/`FindMerged` 出口各调一次（避免双降）。

### 语义查重（embed）
- `pkg/imprint/embed.go`：`Embedder` 接口 + 常量；`internal/embed/client.go`：HTTP 客户端（2s 超时、连接池、失败降级）；`internal/plugin/config.go` 的 `ApplyToOpenOptions` 注入（MCP 与 CLI 都走）。
- `rule_vectors` 侧表（BLOB float32）；同步写入（**踩坑**：原本异步 goroutine，CLI 短进程退出即被杀，向量永远不落盘）。
- 测试 `embed_test.go`：stub embedder 覆盖 5 个场景（语义重复被拦/插件挂了仍能写/极性 advisory/向量生命周期/极性检测）。

## 插件仓库（imprint-embed-plugin，独立 git）
`server.py`（/health + /embed，懒加载、≤64 条/次）、`run.sh`、`imprint.plugin.json`（端口 4174）、`calibrate.py`、`samples.py`、`polarity.py`、`scripts/setup.sh`、README。
venv `~/.imprint/plugins/embed/venv`，权重 `~/.cache/imprint/models`。

## 标定的核心发现（推翻原方案假设）
bge-small-zh-v1.5，三组各 20 对：
- 真重复 median 0.823（min 0.652）｜反义冲突 median 0.753（**max 1.000**）｜无关 median 0.528
- **余弦分不清重复与反义**（反义 "Prefer tabs over spaces"↔"Prefer spaces over tabs" = 1.000）。维度实测 **512 不是 384**（文档记错）。
- 判据对 PascalCase 余弦仅 **0.711**。
- 对策：余弦 + **极性守卫**（否定词不对称 / 反义词对 / 同词集词序互换）。守卫命中 18/20 反义对，冲突误杀 70%→10%，无关误杀 0%。阈值定 **0.70**（能拦住判据对的最高值）。
- 双路径：无极性冲突→硬拒（hint 给 reinforce/supersede/reword）；有极性冲突→放行 + `similar[]` advisory，裁决权归 LLM。

## 验证
`go test ./...` 14 包全绿；`cmd/imprint-acceptance` 12/12；`imprint up/down` ×3 零残留；真机端到端：判据对被拦（score 0.7107，candidates 精确）、反义 0.927 放行并带 advisory、无关放行、插件 kill 后写入照常。

## 环境备忘
- Go 工具链不在 PATH：`~/sdk/go1.25.0/bin`（`export PATH=$PATH:~/sdk/go1.25.0/bin`）。`gofmt` 同目录。
- acceptance 需先 `go install ./cmd/imprint ./cmd/imprint-mcp`。
- 本机有 HTTP 代理，curl 访问 127.0.0.1 要加 `--noproxy '*'`。
- 后台常驻进程要用工具的 run_in_background 启动；普通 `nohup &` 会随 shell 退出被回收。

## 架构评审（Kimi K3 / 程枢，11:30）
- 通读 vault/find/lifecycle/write_guard/store/mcp tools/embed client/polarity/server.py 后完成成熟度评审。
- 新发现 2 个 P0：① imprint.yaml embed 配置漂移（threshold 0.85 会覆盖标定值 0.70，判据对 0.711 必漏网；model 短名 bge-small-zh-v1.5 与 rule_vectors 已存 key "BAAI/..." 分裂）；plugin.json default 0.85、server.py docstring dim 384 同步过期。② conflicts_with 边无任何写入入口——MCP addArgs 与 CLI add 均不暴露 supersedes/related/conflicts（commands.go:144 传 nil），冲突惩罚功能对真实用户不可达。
- P1：find 侧未用向量召回（zero-hit floor 只是保底）；polarity 中文单字 "别" 子串误伤（分别/别人/特别/性别）；add 每次两次 embed 推理可合一。
- P2：Sweep 刷 updated_at 污染 since 语义；O(N) 全库余弦需文档化上限；无 trace_id；版本 dirty 待发布。
- 已确认无问题：DeleteRecord 级联删 rule_vectors；ReinforceRecord 同步 base_confidence；embed client 无重试属 by design。

## 评审问题全修（11:41–12:20，9 项全落地）
- P0-1 配置漂移：imprint.yaml + templates/imprint.yaml + docs/examples/imprint.yaml 三处统一 threshold 0.70 / model BAAI 全名；plugin.json default 0.70；server.py docstring dim 512。注意：`imprint init --force` 会用内嵌模板覆盖根 yaml——embed 块必须同时写进模板，否则被冲掉（已修）。
- P0-2 conflicts 入口：store.AddEdges（校验双端存在、INSERT OR IGNORE、link 事件）；Vault.Link；MCP add 加 conflicts 参数 + 新 link 工具；CLI add --conflicts + 新 link 命令；docs/body.md 同步。测试 link_test.go 3 个。
- P1-3 find 语义召回：rankScore 增加 semVec/vectors 参数，零词法命中且 cosine≥0.55 时 s=sem*confidence 替代地板；findRanked 每次 find 一次 embed，全链路可降级。测试 3 个（提升/保地板/降级）。
- P1-4 polarity 中文“别”：删除单字标记，换 别用/别写/别加/别删/别改/别弄/别碰/别提交；Go+Python 同步；标定重跑 18/20 冲突对不变、0% FPR；中文回归用例 5 个。
- P1-5 add 单次 embed：embedClaim 提取，qvec 复用于 mergeEmbedDuplicatesVec + storeEmbeddingVec；TestAddEmbedsClaimOnce 断言 stub.called==1。
- P2-6 Sweep 不再 bump updated_at（两个 UPDATE 都去掉），list --since 语义恢复。
- P2-7 O(N) 上限写进 mergeEmbedDuplicatesVec 注释（sqlite-vec 方向）。
- P2-8 TraceID：TelemetryEvent.TraceID + newTraceID()（crypto/rand 6B hex）；emit 变 variadic trace；AddRecord/Find/FindMerged/Reinforce/Supersede/Sweep/Forget/Link 贯穿；writer 落 trace_id。trace_test.go 2 个。
- P2-9 PROMPT.md（gitignore 内、未跟踪）移至 docs/PROMPT-v2.1-legacy.md；dist.sh 引用同步。
- 验证：go test ./... 14 包绿；验收 12/12；真机 e2e（/tmp/embed-e2e，已清理）：判据对被拦 score 0.7107、tabs/spaces advisory 0.771、link 后 find 0.8081 vs 0.4041 精确 0.5x、telemetry trace 链 embed_advisory+embed+add 同 trace、down 后 4174 零监听。

## 改名 imprint-embed-plugin → imprint-embed-sidecar（13:35–13:50）
- 动机：「plugin」在 imprint 生态里指 desk 那种完整外部模块；embed 实际是 sidecar 模式 RPC 后端，命名误导。GitHub repo 与本地目录已由用户迁到 imprint-embed-sidecar。
- 改动（主仓库 11 + 插件仓库 3 = 14 处）：imprint.yaml + internal/cli/templates/imprint.yaml + docs/examples/imprint.yaml 三处 package 路径；README.md / README.zh.md 表格行 + GitHub URL（中文 README 顺手补了漏的 Embed 行）；docs/mcp.md + docs/mcp.zh.md 描述里「embed plugin」→「embed sidecar」；internal/cli/templates/body.md agent 模板同步；pkg/imprint/embed.go 与 polarity.go 注释路径；插件仓库 imprint.plugin.json name 字段；插件 README 标题 + 示例 yaml 路径。
- 保留不动：plugin id="embed"（决定部署路径 ~/.imprint/plugins/embed/venv），capabilities=["embed"]，env IMPRINT_PLUGIN_PORT，HTTP /health|/embed 端点。
- 验证：grep embed-plugin 双仓库零匹配；go test ./... 14 包绿（TestEditorRuleMatchesBody 因 body.md 改了需重跑 imprint init --force --cursor）；plugin manifest JSON 合法；真机 e2e（/tmp/sidecar-e2e，已清理）：up 显示「embed · imprint-embed-sidecar ● running」；判据对被拦 score 0.7107；down 后 4174 零监听。
- 未提交。
