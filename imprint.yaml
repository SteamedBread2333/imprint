# Team convention — commit this file.
# Paths are relative to the repo root unless absolute.
# Private runtime lives in gitignored .imprint/ (vault.db, state/, export/).

host:
  listen: 127.0.0.1:9470

telemetry:
  enabled: true
  retention_days: 30

shelves:
  enabled: true
  config:
    roots:
      - docs
      - .cursor/rules   # agent/editor rules — add dirs agents should recall
      # - .cursor/skills

plugins:
  desk:
    enabled: false   # opt-in: clone imprint-desk-plugin, set enabled: true
    package: ../imprint-desk-plugin
    config:
      port: 4173
      hostUrl: http://127.0.0.1:9470